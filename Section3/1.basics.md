### Easiest way of setting up TS project
- npm i -D typescript
- npx tsc --init
- create a index file in root directory
- update tsconfig "outDir": "./dist",
- add below script in package.json
```json
"scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js"
  },
```
